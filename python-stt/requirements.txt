# Meeting Minutes Automator - Python Production Dependencies
# MVP1: Real STT Implementation

# Note: asyncio and queue are Python 3.9+ standard library, intentionally excluded
# to prevent PyPI package override

# STT Engine (MVP1)
faster-whisper>=0.10.0

# Voice Activity Detection (MVP1)
webrtcvad>=2.0.0

# Audio Processing (MVP1)
numpy>=1.24.0

# System Resource Monitoring (MVP1 - Task 3.2)
psutil>=5.9.0

# Note: sounddevice, pyaudio, soundfile are PROHIBITED by ADR-001
# Audio recording is handled exclusively by Rust AudioDeviceAdapter
# Static analysis (check_forbidden_imports.py) enforces this policy

# For development dependencies, see requirements-dev.txt
